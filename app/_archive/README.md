# Combined release archive

`CombinedReleaseV141.tsx` keeps the previous combined page composition outside
the public route tree. The PNG → SVG components and geometry libraries remain
in the repository for a future standalone service. The complete preceding
deployment is also preserved as the previous immutable site version.
